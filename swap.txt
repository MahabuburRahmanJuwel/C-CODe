#include<stdio.h>

int main(){
	char str[30],str1[30];
	int i,temp;
 gets(str);
 for(i=0;str[i]!='\n';i++){
	 str1[i]=str[i];
 }
 str[i]='\0';
 temp=str1[2];
 str1[2]=str1[0];
 str[0]=temp;
 puts(str1);
 return 0;
}
